services:
  api:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./series.db:/app/series.db
    environment:
      DATABASE_URL: "sqlite:///./series.db"
      API_PORT: "8000"
      STREAMLIT_PORT: "8501"
